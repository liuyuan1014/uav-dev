spring:
  application:
    name: service-pilot
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        namespace: dev
        group: UAV_GROUP
        file-extension: yml
        refresh-enabled: true
      discovery:
        server-addr: localhost:8848
        namespace: dev
        group: UAV_GROUP