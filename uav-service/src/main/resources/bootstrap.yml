spring:
  application:
    name: uav-service
  cloud:
    nacos:
      server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yml
    sentinel:
      transport:
        dashboard: localhost:8080
      eager: true
  profiles:
    active: dev

dubbo:
  consumer:
    serialization: fastjson2