<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.uav.service.mapper.PilotAccountMapper">

    <!-- 增加账户余额 -->
    <update id="addAmount">
        UPDATE pilot_account
        SET total_amount = total_amount + #{amount},
            available_amount = available_amount + #{amount},
            total_income_amount = total_income_amount + #{amount},
            update_time = NOW()
        WHERE pilot_id = #{pilotId}
          AND is_deleted = 0
    </update>

    <!-- 减少账户余额 -->
    <update id="reduceAmount">
        UPDATE pilot_account
        SET total_amount = total_amount - #{amount},
            available_amount = available_amount - #{amount},
            total_pay_amount = total_pay_amount + #{amount},
            update_time = NOW()
        WHERE pilot_id = #{pilotId}
          AND available_amount >= #{amount}
          AND is_deleted = 0
    </update>

    <!-- 冻结金额 -->
    <update id="freezeAmount">
        UPDATE pilot_account
        SET lock_amount = lock_amount + #{amount},
            available_amount = available_amount - #{amount},
            update_time = NOW()
        WHERE pilot_id = #{pilotId}
          AND available_amount >= #{amount}
          AND is_deleted = 0
    </update>

    <!-- 解冻金额 -->
    <update id="unfreezeAmount">
        UPDATE pilot_account
        SET lock_amount = lock_amount - #{amount},
            available_amount = available_amount + #{amount},
            update_time = NOW()
        WHERE pilot_id = #{pilotId}
          AND lock_amount >= #{amount}
          AND is_deleted = 0
    </update>

</mapper>