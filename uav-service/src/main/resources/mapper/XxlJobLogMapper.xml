<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.uav.service.mapper.XxlJobLogMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.uav.service.domain.XxlJobLog">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="job_id" property="jobId" jdbcType="INTEGER"/>
        <result column="executor_address" property="executorAddress" jdbcType="VARCHAR"/>
        <result column="executor_handler" property="executorHandler" jdbcType="VARCHAR"/>
        <result column="executor_param" property="executorParam" jdbcType="VARCHAR"/>
        <result column="trigger_time" property="triggerTime" jdbcType="TIMESTAMP"/>
        <result column="trigger_code" property="triggerCode" jdbcType="INTEGER"/>
        <result column="trigger_msg" property="triggerMsg" jdbcType="VARCHAR"/>
        <result column="handle_time" property="handleTime" jdbcType="TIMESTAMP"/>
        <result column="handle_code" property="handleCode" jdbcType="INTEGER"/>
        <result column="handle_msg" property="handleMsg" jdbcType="VARCHAR"/>
        <result column="alarm_status" property="alarmStatus" jdbcType="INTEGER"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!-- 基础列 -->
    <sql id="Base_Column_List">
        id, job_id, executor_address, executor_handler, executor_param,
        trigger_time, trigger_code, trigger_msg, handle_time, handle_code,
        handle_msg, alarm_status, create_time, update_time
    </sql>

    <!-- 根据任务ID查询执行日志 -->
    <select id="selectByJobId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM xxl_job_log
        WHERE job_id = #{jobId}
        AND deleted = 0
        ORDER BY trigger_time DESC
        <if test="limit != null">
            LIMIT #{limit}
        </if>
    </select>

    <!-- 查询失败的执行日志 -->
    <select id="selectFailedLogs" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM xxl_job_log
        WHERE job_id = #{jobId}
        AND handle_code = 500
        AND deleted = 0
        ORDER BY trigger_time DESC
        <if test="limit != null">
            LIMIT #{limit}
        </if>
    </select>

    <!-- 统计任务执行次数 -->
    <select id="countByJobId" resultType="int">
        SELECT COUNT(*)
        FROM xxl_job_log
        WHERE job_id = #{jobId}
        AND deleted = 0
    </select>

    <!-- 统计任务成功次数 -->
    <select id="countSuccessByJobId" resultType="int">
        SELECT COUNT(*)
        FROM xxl_job_log
        WHERE job_id = #{jobId}
        AND handle_code = 200
        AND deleted = 0
    </select>

    <!-- 统计任务失败次数 -->
    <select id="countFailedByJobId" resultType="int">
        SELECT COUNT(*)
        FROM xxl_job_log
        WHERE job_id = #{jobId}
        AND handle_code = 500
        AND deleted = 0
    </select>

</mapper>