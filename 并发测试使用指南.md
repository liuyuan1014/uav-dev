# 🧪 无人机任务并发抢单测试指南

## 📋 测试目的

验证 **Redisson 分布式锁 + MyBatis-Plus 乐观锁** 在高并发场景下的有效性，确保同一个任务只能被一个飞手抢到。

---

## 🚀 快速开始

### 步骤 1：检查 Python 环境

打开命令行（CMD 或 PowerShell），检查 Python 是否已安装：

```bash
python --version
```

**预期输出**：`Python 3.x.x`

如果没有安装 Python，请访问：https://www.python.org/downloads/

---

### 步骤 2：安装依赖库

在项目根目录（`D:/uav/uav-dev`）执行：

```bash
pip install requests
```

**说明**：`requests` 是 Python 的 HTTP 请求库，用于发送 API 请求。

---

### 步骤 3：准备测试数据

#### 3.1 创建一个新任务

使用 Postman 或 curl 创建一个待抢单的任务：

```bash
POST http://localhost:8081/api/mission/publish
Content-Type: application/json

{
  "clientId": 1,
  "title": "并发测试任务",
  "startLocation": "北京",
  "endLocation": "上海",
  "expectedDistance": 1200.5,
  "fee": 500.00
}
```

**记录返回的任务ID**，例如：`"id": 5`

#### 3.2 修改测试脚本配置

打开 `concurrent_mission_test.py`，修改第 13 行：

```python
MISSION_ID = 5  # 改为你刚创建的任务ID
```

---

### 步骤 4：确保服务正常运行

确认以下服务都在运行：

- ✅ **SSH 隧道**：`start-all-tunnels.bat` 正在运行
- ✅ **uav-service**：Spring Boot 应用已启动（端口 8081）
- ✅ **MySQL**：数据库连接正常
- ✅ **Redis**：缓存服务正常

---

### 步骤 5：运行测试

在项目根目录执行：

```bash
python concurrent_mission_test.py
```

---

## 📊 测试结果解读

### 成功的测试结果

```
======================================================================
🚀 无人机任务并发抢单测试
======================================================================
📋 测试配置：
   - API 地址：http://localhost:8081
   - 任务 ID：5
   - 并发数：100 个飞手
   - 开始时间：2026-02-21 23:30:00
======================================================================

🏁 开始并发测试...

✅ 飞手  42 抢单成功！
❌ 飞手   1 抢单失败：任务已被接单
❌ 飞手   2 抢单失败：任务已被接单
... (其他 97 个失败)

======================================================================
📊 测试结果摘要
======================================================================
⏱️  总耗时：2.35 秒
📈 总请求数：100
✅ 成功数：1
❌ 失败数：99
⚠️  异常数：0
======================================================================

======================================================================
🔍 测试验证
======================================================================
✅ 测试通过！
   - 只有 1 个飞手抢单成功
   - Redisson 分布式锁生效
   - MyBatis-Plus 乐观锁生效
   - 并发控制正常
======================================================================
```

### 关键指标

| 指标 | 预期值 | 说明 |
|------|--------|------|
| **成功数** | `1` | 只有 1 个飞手抢单成功 |
| **失败数** | `99` | 其他 99 个飞手抢单失败 |
| **异常数** | `0` | 没有请求异常 |

---

## 🔍 验证步骤

### 1. 数据库验证

连接 MySQL，执行：

```sql
SELECT * FROM uav_mission WHERE id = 5;
```

**预期结果**：
- `status = 1`（已接单）
- `pilot_id = 某个飞手ID`（只有一个）
- `version = 1`（乐观锁版本号增加）

---

### 2. 日志验证

查看 `uav-service` 的控制台输出，应该看到：

**成功的请求**：
```
获取分布式锁成功: mission:5
任务抢单成功: missionId=5, pilotId=42
释放分布式锁: mission:5
```

**失败的请求**：
```
获取分布式锁失败，任务可能已被抢: mission:5
```
或
```
任务已被接单，无法重复接单
```

---

### 3. Redis 验证

打开 Redis 客户端：

```bash
redis-cli -h localhost -p 6379
```

查看锁（测试期间可能看到）：

```redis
KEYS mission:*
```

**说明**：锁会在抢单完成后自动释放，所以测试结束后可能看不到。

---

## ⚠️ 常见问题

### 问题 1：`ModuleNotFoundError: No module named 'requests'`

**原因**：未安装 `requests` 库

**解决**：
```bash
pip install requests
```

---

### 问题 2：所有请求都失败（成功数 = 0）

**可能原因**：
1. 任务ID不存在
2. 任务已被接单（status != 0）
3. 服务未启动

**解决**：
1. 检查任务ID是否正确
2. 创建一个新任务
3. 确认 `uav-service` 正在运行

---

### 问题 3：多个请求成功（成功数 > 1）

**原因**：并发控制失败！

**排查步骤**：
1. 检查 Redis 是否正常运行
2. 检查 Redisson 配置是否正确
3. 检查 MyBatis-Plus 乐观锁插件是否配置
4. 查看应用日志，确认锁是否正常获取和释放

---

### 问题 4：请求超时（异常数 > 0）

**原因**：服务响应慢或网络问题

**解决**：
1. 检查服务是否正常运行
2. 检查数据库连接是否正常
3. 适当增加超时时间（修改脚本第 67 行）

---

## 🎯 测试场景扩展

### 场景 1：调整并发数

修改脚本第 14 行：

```python
THREAD_COUNT = 200  # 增加到 200 个并发
```

### 场景 2：测试多个任务

创建多个任务，依次测试：

```python
MISSION_ID = 5  # 第一个任务
# 运行测试

MISSION_ID = 6  # 第二个任务
# 再次运行测试
```

### 场景 3：压力测试

连续运行多次测试，观察系统稳定性：

```bash
for i in {1..10}; do
  echo "第 $i 次测试"
  python concurrent_mission_test.py
  sleep 2
done
```

---

## 📝 测试报告模板

```
测试时间：2026-02-21 23:30:00
测试任务ID：5
并发数：100
总耗时：2.35 秒

测试结果：
- 成功数：1 ✅
- 失败数：99 ✅
- 异常数：0 ✅

数据库验证：
- 任务状态：已接单 ✅
- 接单飞手：pilot_id = 42 ✅
- 版本号：version = 1 ✅

结论：
✅ 测试通过！Redisson 分布式锁和 MyBatis-Plus 乐观锁在高并发场景下工作正常。
```

---

## 🔗 相关文档

- [Redisson 官方文档](https://github.com/redisson/redisson/wiki)
- [MyBatis-Plus 乐观锁](https://baomidou.com/pages/0d93c0/)
- [Python requests 文档](https://requests.readthedocs.io/)

---

## 💡 提示

- 每次测试前，确保创建一个新的待抢单任务（status = 0）
- 测试完成后，可以查看数据库和日志进行验证
- 如果测试失败，检查 Redis 和数据库连接是否正常
- 建议在测试环境进行压力测试，避免影响生产数据

---

**祝测试顺利！** 🎉